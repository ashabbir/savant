#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative '../lib/savant/db'

begin
  db = Savant::DB.new
  db.migrate_tables
  db.ensure_fts
  puts 'DB SMOKE OK'
  exit 0
rescue => e
  warn "DB SMOKE ERROR: #{e.class}: #{e.message}"
  exit 1
end

