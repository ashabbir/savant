#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative '../lib/savant/framework/db'

begin
  db = Savant::Framework::DB.new
  db.ensure_fts
  puts 'DB FTS READY'
  exit 0
rescue StandardError => e
  warn "DB FTS ERROR: #{e.class}: #{e.message}"
  exit 1
end
