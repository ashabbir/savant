POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=contextdb
POSTGRES_USER=context
POSTGRES_PASSWORD=changeme

# Preferred single-variable connection string (overrides POSTGRES_*):
# DATABASE_URL=postgres://context:changeme@localhost:5432/contextdb

MCP_LISTEN_HOST=0.0.0.0
MCP_LISTEN_PORT=8765
SAVANT_PATH=./

# Jira (set either Cloud with email+token, or Server with username+password)
JIRA_BASE_URL=https://your-domain.atlassian.net
JIRA_EMAIL=
JIRA_API_TOKEN=
JIRA_USERNAME=
JIRA_PASSWORD=
